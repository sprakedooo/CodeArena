{
  "name": "codearena",
  "version": "2.0.0",
  "description": "CodeArena - AI-Powered Game-Based Adaptive Learning System with Decision Tree ML",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently --names \"API,WEB\" -c \"bgBlue,bgGreen\" \"npm run backend\" \"npm run frontend\""
  },
  "keywords": ["codearena", "ai", "learning", "game", "adaptive", "decision-tree", "ml"],
  "author": "CodeArena Team",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
